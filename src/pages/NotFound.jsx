import React from "react";
import styled from "styled-components";

const NotFound = () => {
  return <StNotFoundWrap>찾을 수 없는 페이지 입니다.</StNotFoundWrap>;
};

export default NotFound;

const StNotFoundWrap = styled.div`
  width: 100%;
`;
